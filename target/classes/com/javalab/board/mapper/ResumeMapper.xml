<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.javalab.board.mapper.ResumeMapperInterface">
    <insert id="insertResume">
        INSERT INTO resumes (resumeId, jobSeekerId, title, content, education, experience, link, hitNo, fileName, filePath)
        VALUES (#{resumeId}, #{jobSeekerId}, #{title}, #{content}, #{education}, #{experience}, #{link}, #{hitNo}, #{fileName}, #{filePath})
    </insert>

    <select id="selectResumeById" resultType="com.javalab.board.vo.ResumeVo">
        SELECT * FROM resumes WHERE resumeId = #{resumeId}
    </select>

    <select id="selectAllResumes" resultType="com.javalab.board.vo.ResumeVo">
        SELECT * FROM resumes
    </select>

    <update id="updateResume">
        UPDATE resumes
        SET jobSeekerId = #{jobSeekerId}, title = #{title}, content = #{content}, education = #{education},
            experience = #{experience}, link = #{link}, hitNo = #{hitNo}, fileName = #{fileName}, filePath = #{filePath}
        WHERE resumeId = #{resumeId}
    </update>

    <delete id="deleteResume">
        DELETE FROM resumes WHERE resumeId = #{resumeId}
    </delete>
</mapper>
